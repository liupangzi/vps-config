server {
    listen 80;
    server_name gobble.chaos.plus;
    access_log /var/log/nginx/gobble_chaos_plus.access.log main;
    rewrite ^/(.*)$  https://gobble.chaos.plus/$1 permanent;
}

server {
    listen 443 ssl http2;
    server_name gobble.chaos.plus;

    root /var/www/chaos.plus/gobble;
    index index.html;

    access_log /var/log/nginx/gobble_chaos_plus.access.log main;
    error_log  /var/log/nginx/gobble_chaos_plus.error.log warn;

    include servers/chaos.plus/common/https.conf;
}
